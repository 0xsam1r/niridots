#  _                      _            _    
# | |__  _   _ _ __  _ __| | ___   ___| | __
# | '_ \| | | | '_ \| '__| |/ _ \ / __| |/ /
# | | | | |_| | |_) | |  | | (_) | (__|   < 
# |_| |_|\__, | .__/|_|  |_|\___/ \___|_|\_\
#        |___/|_|                           
# 

# source = ~/.cache/wallust/hyprlock-colors.conf

# sample hyprlock.conf
# for more configuration options, refer https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock
#
# rendered text in all widgets supports pango markup (e.g. <b> or <i> tags)
# ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#general-remarks
#
# shortcuts to clear password buffer: ESC, Ctrl+U, Ctrl+Backspace
#

$font = JetBrainsMono Nerd Font
$font_color = rgb(143, 143, 143)

general {
    hide_cursor = true
    # skips validation when no password is provided 
    ignore_empty_input = true
    immediate_render = true

}

# uncomment to enable fingerprint authentication
# auth {
#     fingerprint {
#         enabled = true
#         ready_message = Scan fingerprint to unlock
#         present_message = Scanning...
#         retry_delay = 250 # in milliseconds
#     }
# }

animations {
    enabled = true
    bezier = linear, 1, 1, 0, 0
    animation = fadeIn, 1, 5, linear
    animation = fadeOut, 1, 5, linear
    animation = inputFieldDots, 1, 2, linear
}

background {
    monitor =
    path = screenshot # indicate that wallpaper is screenshot THX Hyprland
    blur_passes = 1
}

input-field {
    monitor =
    size = 20%, 5%
    outline_thickness = 0 # remove boarder 
    inner_color = rgba(0, 0, 0, 0.0) # no fill
    

    # i don't know what they do 
    #outer_color = rgba(33ccffee) rgba(00ff99ee) 45deg
    #check_color = rgba(00ff99ee) rgba(ff6633ee) 120deg
    #fail_color = rgba(ff6633ee) rgba(ff0066ee) 40deg

    font_color = $font_color
    fade_on_empty = false
    rounding = 20

    font_family = $font
    placeholder_text = Input password...
    fail_text = $PAMFAIL # pam auth last fail reason

    # uncomment to use a letter instead of a dot to indicate the typed password
    # dots_text_format = *
    # dots_size = 0.4
    dots_spacing = 0.3

    # uncomment to use an input indicator that does not show the password length (similar to swaylock's input indicator)
    #hide_input = true

    # position at center
    position = 0, 0
    halign = center
    valign = center
}

# TIME
label {
    monitor =
    text = $TIME # ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#variable-substitution
    font_size = 90
    font_family = $font

    position = -30, 0
    halign = right
    valign = top
}

# DATE
label {
    monitor =
    text = cmd[update:60000] date +"%A, %d %B %Y" # update every 60 seconds
    font_size = 25
    font_family = $font

    position = -30, -150
    halign = right
    valign = top
}

# Keyboard Layout
label {
    monitor =
    
    font_size = 24
    text = $LAYOUT
    
    #onclick = hyprctl switchxkblayout all 0

    position = 0, 0
    halign = left
    valign = bottom
}
